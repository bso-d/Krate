# Kafbat UI login credentials
# Change these before running `kafka install` in production.
KAFKA_UI_USER=admin
KAFKA_UI_PASSWORD=changeme

# Host port for Kafbat UI (nginx proxy). Default: 8080
# Change if 8080 is already in use on the VM.
KAFKA_UI_PORT=8080
