# credentials — never commit
.env
zk/.env
kraft/.env

# reference files not needed in repo
docker-compose-ref.yml

# built bundles and saved docker images
dist/
images/
docker-offline/
security-reports/
.DS_Store
